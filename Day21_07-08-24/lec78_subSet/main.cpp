#include <iostream> 
#include <vector>

using namespace std;

class Solution {
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>> result();
        
        vector<int> subset();
        int counter = 0; 
        
        while(counter <= nums.size()){
            
        }
    }

 

};

int main(){


    return 0;
}